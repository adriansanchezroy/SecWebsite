<!DOCTYPE html>

<html lang="en">

  <head>
    <title>Clients Affaires</title>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" href="css/styles.css">




  </head>

  <body>
  
    <%- include('header') %>

  <br>
<div class="container-lg mt-3">
  <header>
    <h1>Clients Affaires</h1>
    </header>
    <main>
    <table class="table table-striped table-bordered">
        <thead>
          <tr>
            <th>Prénom</th>
            <th>Nom</th>
            <th>Rôles</th>
          </tr>
        </thead>
        <tbody>
          <% users.forEach((user) => { %>
              <% user.roles.forEach((role) => { %>
                    <% if(user.roles == "business") { %>
                    <tr>
                        <td><%= user.firstName %></td>
                        <td><%= user.lastName %></td>
                        <td><%= user.roles %></td>
                    </tr>

                <% } %>
                <% }) %>
                <% }) %>

        </tbody>
      </table>
    </main>
  </div>
    <%- include('footer') %> <!-- Inclure le footer -->
</body>
</html>